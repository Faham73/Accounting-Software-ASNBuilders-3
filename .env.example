# Database
# PostgreSQL connection string
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/accounting_db

# Optional: Direct database URL for migrations (if using connection pooling)
# DIRECT_URL=postgresql://user:password@host:port/database

# Authentication
# Secret key for JWT token signing (generate a secure random string)
# In production, use a strong random secret: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production

# Application URL (optional, auto-detected on Vercel)
# Used for PDF generation and absolute URLs
APP_URL=http://localhost:3000
# Or use NEXT_PUBLIC_APP_URL for client-side access
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Base path if deploying under a subpath
# NEXT_PUBLIC_BASE_PATH=/accounting

# Optional: Chrome executable path (for local development with Puppeteer)
# CHROME_EXECUTABLE_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe

# Development only - do not set in production
# NODE_ENV=development